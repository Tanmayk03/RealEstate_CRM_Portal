# Local dev: leave VITE_API_URL unset so the app uses the Vite proxy (see vite.config.js).
# Only set this if you skip the proxy, e.g. VITE_API_URL=http://127.0.0.1:8088
#
# And in vite.config, set VITE_PROXY_TARGET to the same for `npm run dev`.
